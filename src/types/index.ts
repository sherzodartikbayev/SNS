export interface ProgressBarProps {
  title: string;
  link: string;
  point: string;
}

export interface ButtonProps {
  title: string;
  link?: string;
  img: string;
  alt: string;
  color: string;
}